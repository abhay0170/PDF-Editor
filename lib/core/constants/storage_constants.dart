/// Subfolder names within the app's managed documents directory
/// (`getApplicationDocumentsDirectory()`).
class StorageConstants {
  const StorageConstants._();

  static const String documentsSubdir = 'documents';
  static const String thumbnailsSubdir = 'thumbnails';
  static const String databaseFileName = 'pdf_reader';
}
